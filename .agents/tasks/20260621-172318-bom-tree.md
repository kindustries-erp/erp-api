# Multi-level BOM visualization in MO

## Scope
- Update `explodeBom` to return a hierarchical tree.
- Add `GET /api/v1/production/explode-preview` endpoint for frontend.
- Persist `outputMetadata.explosionTree` for MOs.
