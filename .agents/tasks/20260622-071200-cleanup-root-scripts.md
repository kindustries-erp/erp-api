# Cleanup Root Scripts

## Objective
Organize and clean up adhoc scripts from the root directory of the `liouni-erp-api` repository.

## Changes
- Moved `*.js`, `*.sql`, `*.ps1` test and adhoc scripts to `scripts/adhoc/`.
- Deleted temporary text output and log files (`*.txt`, `*.log`).
- Committed changes to `.github/workflows/cd-erp-klotus-master.yml` modified by the user.

## Status
Completed.
