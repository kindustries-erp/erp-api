name: Pipeline API — erp-greenway-production

on:
  push:
    branches:
      - erp-greenway-production
  pull_request:
    branches:
      - erp-greenway-production

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/kindustries-erp/erp-api-greenway-production
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  ci:
    name: Lint, Type & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check (type + lint + prettier)
        run: bun run check:ci

      - name: Test
        run: bunx jest --forceExit

      - name: Build
        run: bun run build

  build-and-push:
    name: Build & Push to GHCR
    runs-on: ubuntu-latest
    needs: ci
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    outputs:
      sha: ${{ steps.meta.outputs.sha }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract short SHA
        id: meta
        run: echo "sha=${GITHUB_SHA::8}" >> $GITHUB_OUTPUT

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Head
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name == 'push'

    steps:
      - name: Connect Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_CLIENT_SECRET }}
          tags: tag:ci

      - name: Install sshpass
        run: sudo apt-get update && sudo apt-get install -y sshpass

      - name: Guard deploy secrets
        run: |
          test -n "${{ secrets.GHCR_PAT }}" || (echo "GHCR_PAT missing" && exit 1)
          test -n "${{ secrets.HEAD_PASSWORD }}" || (echo "HEAD_PASSWORD missing" && exit 1)

      - name: Write .env to Head
        env:
          SSHPASS: ${{ secrets.HEAD_PASSWORD }}
        run: |
          sshpass -e ssh -o StrictHostKeyChecking=no \
            -p ${{ vars.HEAD_PORT }} \
            ${{ vars.HEAD_USER }}@${{ vars.HEAD_HOST }} \
            "mkdir -p /home/dev/opt/stacks/erp/erp-api-greenway-production && cat > /home/dev/opt/stacks/erp/erp-api-greenway-production/.env" \
            <<< "${{ secrets.GREENWAY_PRODUCTION_API_ENV }}
          R2_ACCOUNT_ID=${{ secrets.GREENWAY_PRODUCTION_R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID=${{ secrets.GREENWAY_PRODUCTION_R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY=${{ secrets.GREENWAY_PRODUCTION_R2_SECRET_ACCESS_KEY }}
          R2_BUCKET_NAME=${{ secrets.GREENWAY_PRODUCTION_R2_BUCKET_NAME }}"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Transfer Image to Head
        env:
          SSHPASS: ${{ secrets.HEAD_PASSWORD }}
        run: |
          docker pull ${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.sha }}
          docker save ${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.sha }} | \
            sshpass -e ssh -o StrictHostKeyChecking=no \
            -p ${{ vars.HEAD_PORT }} \
            ${{ vars.HEAD_USER }}@${{ vars.HEAD_HOST }} \
            "docker load"

      - name: Run deploy script
        env:
          SSHPASS: ${{ secrets.HEAD_PASSWORD }}
        run: |
          sshpass -e ssh -o StrictHostKeyChecking=no \
            -p ${{ vars.HEAD_PORT }} \
            ${{ vars.HEAD_USER }}@${{ vars.HEAD_HOST }} \
            "HOST_PORT=\$(grep -E '^HOST_PORT=' /home/dev/opt/stacks/erp/erp-api-greenway-production/.env | cut -d '=' -f2 | tr -d '\r' | tr -d ' ' || true) ; \
             if [ -z \"\$HOST_PORT\" ]; then HOST_PORT=10014; fi ; \
             docker rm -f erp-api-greenway-production || true ; \
             docker rm -f liouni-erp-core-api-greenway || true ; \
             docker ps -q --filter publish=\$HOST_PORT | xargs -r docker rm -f || true ; \
             docker run -d --name erp-api-greenway-production --restart always --env-file /home/dev/opt/stacks/erp/erp-api-greenway-production/.env -e PORT=3000 -p \$HOST_PORT:3000 ${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.sha }}"

      - name: Health check
        env:
          SSHPASS: ${{ secrets.HEAD_PASSWORD }}
        run: |
          sshpass -e ssh -o StrictHostKeyChecking=no \
            -p ${{ vars.HEAD_PORT }} \
            ${{ vars.HEAD_USER }}@${{ vars.HEAD_HOST }} \
            '
              HOST_PORT=$(grep -E "^HOST_PORT=" /home/dev/opt/stacks/erp/erp-api-greenway-production/.env | cut -d "=" -f2 | tr -d "\r" | tr -d " " || true)
              if [ -z "$HOST_PORT" ]; then HOST_PORT=10014; fi
              echo "🔍 Health check on port $HOST_PORT (waiting 15s for container warm-up...)"
              sleep 15
              for i in $(seq 1 30); do
                code=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:$HOST_PORT/api/v1/auth/profile || true)
                if [ "$code" = "401" ] || [ "$code" = "200" ]; then
                  echo "✅ API health OK (attempt $i)"
                  docker image prune -af
                  exit 0
                fi
                echo "Attempt $i/30: got $code, retrying in 5s..."
                sleep 5
              done
              echo "❌ Health check failed after 30 attempts ($(( 15 + 30 * 5 ))s total)"
              docker logs --tail 50 erp-api-greenway-production || true
              exit 1
            '

